var arr = ["dav","tiw","ans","hu"]

//map is a method that returns a new array with the same length as the original array
console.log(arr.map(data=>data))

//splice is a method that removes the elements from the array
console.log(arr.splice(1,2))

//split is a method that splits the string into an array
console.log(arr.split(""))

